#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "");
    char fold;
    int c, d;
    cout << "Jakie działanie chcesz wykonać?" << endl;
    cin >> decyzja;
    if (decyzja == "dodawanie")
        dodawanie(c, e);
    else if (decyzja == "odejmowanie")
        dzielenie(e, d);
    else if (decyzja == "mnozenie")
        mnozenie(c, d);
    else if (decyzja == "dzielenie")
        mnozenie(c, d);
    return 1;

}

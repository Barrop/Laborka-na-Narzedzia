#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "");
    float a, b;
    cout << "Podaj A: " << endl;
    cin >> a;
    cout << "Podaj B: " << endl;
    cin >> b;

    if (b == 0)
		{
			cout << "Nie wolno dzielić przez zero!" << endl;
		}
    else
		{
			cout << "Iloraz: " << (a / b) << endl;
		}
    return 0;

}
//dsfsdf
